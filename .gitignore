# Node/JavaScript build artifacts and dependencies
node_modules

# Expo Cache and Build Artifacts
.expo
.expo-shared
.expo-cache
build/
*.log

# React Native / Metro Bundler
metro-cache
coverage

# TypeScript compilation output
dist

# macOS
.DS_Store

# Android specific files (if using native build paths)
android/build
android/.gradle

# iOS specific files (if using native build paths)
iOS/build
iOS/*.xcworkspace
iOS/*.xcodeproj

# IDE Artifacts
.idea/
*.swp

# Secrets and Environment Variables (Important for mobile)
.env
*.env

# Testing artifacts
junit.xml
